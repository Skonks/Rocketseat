/* 
    Operador de String
*/

// Comparação
console.log("a" === "b")

// Concatenação
console.log("Lin"+"da")

// Retorna a união de duas Strings
let alpha = "alpha"
// alpha += "bet" (É bom caso eu queira só atualizar a string direto(alpha = alpha + "bet"))
console.log(alpha + "bet")